# OpenClaw Enterprise Control Plane - Environment Variables

# API key for authenticating requests to the control plane (optional, set auth.required=true in config)
CONTROL_PLANE_API_KEY=your-secret-api-key

# Portkey AI Gateway API key (used for cheap + premium LLM routing)
PORTKEY_API_KEY=your-portkey-api-key

# Override config file path (default: config/default.json)
# CONFIG_PATH=config/default.json
